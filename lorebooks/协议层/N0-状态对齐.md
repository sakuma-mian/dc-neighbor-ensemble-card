# N0 · 协议层·状态对齐 — 世界书源

> 对接件：剧情侧 ↔ 变量侧（本卡无备忘卡的记忆缺口在此补上）｜契约增量提案，见同目录 README §2/§9

## 条目（ENT-050）

```yaml
entryId: ENT-050
comment: "[mvu_plot]协议层·状态对齐"
activation: constant
recursion: non_recursable
position: 0
order: 210
gating: none
```

**content：**

【协议层·状态对齐】本条目是剧情侧与变量侧的对接件，每轮常驻。

一、世界观口径：本故事为原创现代都市叙事（南江市，当代），没有原著作品与原作剧情线。一切涉及「原著／原作锚点／蝴蝶效应」的检查一律按无原著模式执行：只对照本卡已确立的事实与因果链，不做任何原作比对。

二、记忆素材约定：本卡未开启绘绘「备忘·状态持久层」。思维链步.0.5（主角行踪回顾）与步.1（故事进度定位）所需的记忆素材＝聊天历史＋下方状态块（user／环境／登场／关系／任务容器），不要去翻不存在的备忘卡。

三、当前状态（只读参考，不要在正文里输出或复述本块）：
<status_current_variables>
{{format_message_variable::stat_data}}
</status_current_variables>
迷雾表读法：知情者 → 事实ID → 知情状态（未揭示／已揭示／误信已纠正）；某角色不出现自身前缀的格子。这张表只告诉你"谁知道什么"，不含秘密内容；秘密内容只随已启用的秘密词条进入上下文。据此执行 INC-1 保密纪律与 INC-2 群像调度。

四、【状态更新】标记的归属：输出规范末尾若出现「【状态更新】」——当你的上下文里存在「[mvu_update]变量输出格式」词条（单模型模式），在回复最末按该词条输出 <UpdateVariable> 块，它是整个回复的最后一个块；当该词条不在上下文（双模型模式），不要输出任何变量块，状态结算由独立更新通道负责。

## 溯源与取舍

- 机制来源：`{{format_message_variable::stat_data}}` 宏与 `<status_current_variables>` 包装沿用变量层 `[mvu_update]变量列表`（DEC-024 抄定件，re0 机制文本）；世界观口径承接 DEC-001（南江市原创）与设计契约 §①
- 取舍：世界观声明并入本条而非单开条目——它只服务于"通用思维链按无原著模式跑"这一个目的；单模型模式下与 `[mvu_update]变量列表` 重复注入同一状态，组装门可测合并
- 风险：全表序列化成本与事实 ID 话题暴露面，见 README §8.1/8.2
