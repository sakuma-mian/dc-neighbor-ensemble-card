# 世界观层 · 《但为君故》南江市

> 层性质：**跨卡共享**（DEC-014）。本目录内容供本卡与后续同世界观同伴卡共同引用；**不得写死在单卡私有层**。
> 状态：**提案稿（Proposed）**——除已标注「已定」的决策外，其余内容待主人逐项确认。
> 生成物：本目录 Markdown 是**维护源**，稿件将转为卡内世界书条目（见各文件的“条目化”小节）。
> 规范依据：`ST-A3`《世界书优化》v1.0.1（已读）。**所有条目字段取值须遵守该指南；命中其 §21 悬案（U1–U11）的机制一律不用。**

---

## 本目录文件

| 文件 | 内容 | 对应条目层 |
|---|---|---|
| `W1-面纱规则.md` | 面纱的第一硬规则、目击后果、三态、叙事纪律 | 世界规则层 |
| `W2-异能体系.md` | 觉醒机制、四档阶段、代价三类、越界与过载、反万能条款 | 世界规则层 |
| `W3-南江市.md` | 城市定位、三个可用作舞台的特殊空间、当前时代压力 | 世界规则层 / 场所层 |
| `W4-组织格局.md` | 核心组织（共生型基金会）＋边缘组织群像 | 组织层 |
| `W5-场所层.md` | 六城区与专项空间（含隐秘面）＋素材冲突裁决 | 场所层 |
| `W6-源流与典籍.md` | 旧神学院、典籍与组织化的百年源流 | 世界规则层 / 前史 |

---

## 目录结构（本案最终形态，尚未创建）

```text
但为君故/
├─ creative-authority.md      ← 决策权威（已存在）
├─ design-contract.md         ← 本卡设计契约（已存在）
├─ worldview/                 ← 跨卡共享世界观层（本目录）
└─ cards/<卡名>/              ← 单卡私有层（尚未创建）
   ├─ schema/                 ← Zod schema
   ├─ states/                 ← 初始变量
   ├─ update-rules/           ← 变量更新规则与输出格式
   ├─ lorebooks/              ← 角色层/场所层词条源
   ├─ scripts/                ← MVU 装载、渲染正则
   └─ card-source.json        ← 卡源（身份字段/开场/示例）
```

---

## 世界书投递总策略（本世界观层）

| 项 | 取值 | 依据 |
|---|---|---|
| 预算 | 世界书预算默认 **25%**；本层目标：常驻条目 ≤ 400 字，按需条目 ≤ 700 字/条 | `ST-A3` §8 |
| 常驻（Constant）用量 | **严格限量**：全卡常驻内容合计目标 ≤ 600 字，只放“不常驻就会崩”的铁律 | `ST-A3` §3 避坑（大量 Constant 会挤占 Conditional） |
| 中文条目 | **必须关闭 Match Whole Words**（全词匹配对中文有破坏性影响），建议条目级设置 | `ST-A3` §4.3 |
| 激活策略 | 默认 🟢 Conditional；仅铁律用 🔵 Constant | `ST-A3` §3 |
| 插入位置 | 世界规则/组织：`position=1`（After Char Defs）；场景与临场细节：`position=4`（At Depth，`depth=4`、`role=0`） | `ST-A3` §6、§17 |
| 递归 | 允许用于“城市 → 城区 → 场所”链；静态设定条目设 Non-recursable 防被误激活 | `ST-A3` §11 |
| 不使用的机制 | `probability` 随机门（沙盒需确定性）、`Inclusion Group` 加权随机、向量化匹配、Automation ID、`@@activate` 装饰器 | `ST-A3` §3/§9/§12/§14/§15 与 §21 悬案 |

> **预算口径诚实声明**：以上字数为**起草估算**，不是实测 token 数；真实占用须在组装阶段用实际编码实测后回填（对应离线验收第 8 项）。
